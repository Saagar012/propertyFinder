'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {
      // This query alters the existing ENUM type by adding the new value 'REJECTED'
      // Make sure the ENUM type name matches the one generated by Sequelize.
      await queryInterface.sequelize.query(`
        ALTER TYPE "property_status" ADD VALUE 'REJECTED'
      `);
    },

  async down (queryInterface, Sequelize) {
    // Down migrations for ENUM changes can be complicated because PostgreSQL does not support
    // removing a value from an ENUM type directly. In many cases, the down migration is left irreversible.
    return Promise.resolve();

  }
};
